import { LimitDatasetNumbersPipe } from './limit-dataset-numbers.pipe';

describe('LimitDatasetNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitDatasetNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
